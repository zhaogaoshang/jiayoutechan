<!--pages/order/order.wxml-->
<view class="container order">
<!-- <construction></construction> -->
  <view class="category-box">
    <view class="category-item" wx:for="{{category}}" wx:key="{{index}}" catchtap="handleSwichCategory" data-id="{{item.id}}" data-status="{{item.status}}">
      <view>{{item.name}}</view>
      <view class="line" wx:if="{{item.id==activeCategory}}"></view>
    </view>
  </view>
<block wx:for="{{allParams}}" wx:key="{{index}}" wx:if="{{item.initParam.composite_status == activeStatus}}">
  <view class="order-item" catchtap="handleOrderInfo" wx:for="{{item.lists.order_list}}" wx:key="{{index}}" data-order-id="{{child.order_id}}" wx:for-item="child">
    <view class="tiao-box">
      <view class="tiao-box__left" style="color:#999;font-size:24rpx;">订单号：{{child.order_sn}}</view>
      <view class="tiao-box__middle"></view>
      <view class="tiao-box__right">{{child.handler}}</view>
    </view>
    <view>
      <view class="product-item" wx:for="{{child.goods_list}}" wx:key="{{index}}">
        <product-item imgH="{{144}}" imgW="{{144}}" item="{{item}}"></product-item>
        <view class="product-count">{{item.goods_attr}}</view>
        <view class="product-price">￥{{item.formated_goods_price}}</view>
        <view class="product-container__text">x{{item.goods_number}}</view>
      </view>
      <view class="bill-box">
        共{{child.count}}件商品  小计：<text>￥{{child.total_fee}} </text>(含运费￥{{child.fee_amount}})
      </view>
      <view class="handle-btn">
        <!-- <view>共100件商品 小计：<text>100元</text>(含运费10元)</view> -->
        <!--待支付-->
        <block wx-if="{{child.extension_code == 10}}">
          <button class="handle-btn__red">立即支付</button>
        </block>
        <block wx-if="{{child.extension_code == 20}}">
          <button class="handle-btn__red">申请退款</button>
        </block>       
        <block wx-if="{{child.extension_code == 30}}">
          <button>申请退货</button>
          <button>查看物流</button>
          <button class="handle-btn__red">确认收货</button>
        </block>          
        <block wx-if="{{child.extension_code == 40}}">
          <button>申请售后</button>
          <button class="handle-btn__red">立即评价</button>
        </block>              
      </view>
    </view>
  </view>
</block>
  <!-- 全部订单 -->
  <!-- <view class="order-item" wx:if="{{activeCategory=='all'}}" catchtap="handleOrderInfo" wx:for="{{allOrder.list}}" wx:key="{{index}}" data-order-id="{{item.order_id}}">
    <view class="tiao-box">
      <view class="tiao-box__left"></view>
      <view class="tiao-box__middle"></view>
      <view class="tiao-box__right">待支付</view>
    </view>
    <view>
      <view class="product-item" wx:for="{{product}}" wx:key="{{index}}">
        <product-item imgH="{{144}}" imgW="{{144}}" item="{{item}}"></product-item>
        <view class="product-count">4箱草莓+3箱苹果+1箱栗子</view>
        <view class="product-price">75.00</view>
        <view class="product-container__text">x1</view>
      </view>
      <view class="handle-btn">
        <button>取消订单</button>
        <button class="handle-btn__red">立即支付</button>
      </view>
    </view>
  </view> -->

  <!-- 待支付 -->
  <!-- <view class="order-item" wx:if="{{activeCategory=='daiZhiFu'}}">
    <view class="tiao-box">
      <view class="tiao-box__left">鲜公旗舰店</view>
      <view class="tiao-box__middle"></view>
      <view class="tiao-box__right">待支付</view>
    </view>
    <view>
      <view class="product-item" wx:for="{{product}}" wx:key="{{index}}">
        <product-item imgH="{{144}}" imgW="{{144}}" item="{{item}}"></product-item>
        <view class="product-count">4箱草莓+3箱苹果+1箱栗子</view>
        <view class="product-price">75.00</view>
        <view class="product-container__text">x1</view>
      </view>
      <view class="handle-btn">
        <button>取消订单</button>
        <button class="handle-btn__red">立即支付</button>
      </view>
    </view>
  </view> -->

  <!-- 待发货 -->
  <!-- <view class="order-item" wx:if="{{activeCategory=='daiFaHuo'}}">
    <view class="tiao-box">
      <view class="tiao-box__left">鲜公旗舰店</view>
      <view class="tiao-box__middle"></view>
      <view class="tiao-box__right">待支付</view>
    </view>
    <view>
      <view class="product-item" wx:for="{{product}}" wx:key="{{index}}">
        <product-item imgH="{{144}}" imgW="{{144}}" item="{{item}}"></product-item>
        <view class="product-count">4箱草莓+3箱苹果+1箱栗子</view>
        <view class="product-price">75.00</view>
        <view class="product-container__text">x1</view>
      </view>
      <view class="handle-btn">
        <button>取消订单</button>
        <button class="handle-btn__red">立即支付</button>
      </view>
    </view>
  </view> -->

  <!-- 待收货 -->
  <!-- <view class="order-item" wx:if="{{activeCategory=='daiShouHuo'}}">
    <view class="tiao-box">
      <view class="tiao-box__left">鲜公旗舰店</view>
      <view class="tiao-box__middle"></view>
      <view class="tiao-box__right">待支付</view>
    </view>
    <view>
      <view class="product-item" wx:for="{{product}}" wx:key="{{index}}">
        <product-item imgH="{{144}}" imgW="{{144}}" item="{{item}}"></product-item>
        <view class="product-count">4箱草莓+3箱苹果+1箱栗子</view>
        <view class="product-price">75.00</view>
        <view class="product-container__text">x1</view>
      </view>
      <view class="handle-btn">
        <button>取消订单</button>
        <button class="handle-btn__red">立即支付</button>
      </view>
    </view>
  </view> -->

  <!-- 待评价 -->
  <!-- <view class="order-item" wx:if="{{activeCategory=='daiPingJia'}}" wx:for="{{allOrder.list}}" wx:key="{{index}}">
    <view class="tiao-box">
      <view class="tiao-box__left">鲜公旗舰店</view>
      <view class="tiao-box__middle"></view>
      <view class="tiao-box__right">待支付</view>
    </view>
    <view>
      <view class="product-item" wx:for="{{product}}" wx:key="{{index}}">
        <product-item imgH="{{144}}" imgW="{{144}}" item="{{item}}"></product-item>
        <view class="product-count">4箱草莓+3箱苹果+1箱栗子</view>
        <view class="product-price">75.00</view>
        <view class="product-container__text">x1</view>
      </view>
      <view class="handle-btn">
        <button class="handle-btn__red" data-id="{{item.order_id}}" data-order-id="{{item.order_sn}}" data-rec-id="{{item.rec_id}}" catchtap="handleGoComment">评价</button>
      </view>
    </view>
  </view> -->

  <!-- 退款退货 -->
  <!-- <view class="order-item" wx:if="{{false}}">
    <view class="tiao-box">
      <view class="tiao-box__left">鲜公旗舰店</view>
      <view class="tiao-box__middle"></view>
      <view class="tiao-box__right">待支付</view>
    </view>
    <view>
      <view class="product-item" wx:for="{{product}}" wx:key="{{index}}">
        <product-item imgH="{{144}}" imgW="{{144}}" item="{{item}}"></product-item>
        <view class="product-count">4箱草莓+3箱苹果+1箱栗子</view>
        <view class="product-price">75.00</view>
        <view class="product-container__text">x1</view>
      </view>
      <view class="handle-btn">
        <button>取消订单</button>
        <button class="handle-btn__red">立即支付</button>
      </view>
    </view>
  </view> -->

</view>